# Openned Monorepo Setup Tasks

- `[/]` Workspace Initialization
  - `[ ]` Initialize `docs/` folder and copy architecture artifacts.
  - `[ ]` Configure `pnpm-workspace.yaml`.
  - `[ ]` Setup root `package.json` for Turborepo.
  - `[ ]` Configure `turbo.json`.
- `[ ]` Apps Directory
  - `[ ]` Move existing Next.js code to `apps/web`.
  - `[ ]` Initialize NestJS in `apps/api`.
- `[ ]` Packages Directory
  - `[ ]` Setup `packages/ui`.
  - `[ ]` Setup `packages/config` (eslint, tsconfig).
- `[ ]` Verification
  - `[ ]` Run `pnpm build` across the monorepo.
